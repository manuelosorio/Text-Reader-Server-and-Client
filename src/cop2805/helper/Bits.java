package cop2805.helper;

public class Bits {
// The purpose of this is to convert any commas inside a string (using a constant value)
// before adding into the Hashmap. Doing this will allow me to convert the hashmap to a
// string and later parse the string back to a hashmap and turn the bit
// back to a comma without any issues. Created a separate class in case
// I needed to add more values hear and couldn't get it to work with an enum.
    public static final int COMMA = 1;
}
